name: payload_server
description: PAYLOAD backend (§2.2, §2.3, §2.4) — Serverpod project.
# version: 1.0.0

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  serverpod: 3.4.11
  serverpod_auth_idp_server: 3.4.11
  serverpod_auth_core_server: 3.4.11
  content_schema: ^0.1.0
  sim_core: ^0.1.0

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.11
  test: ^1.25.5

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations
