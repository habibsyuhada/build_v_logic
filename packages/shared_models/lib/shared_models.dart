/// Client<->server DTOs (§2.2). Populated in Phase 4 once Serverpod
/// generates the real protocol classes from `server/protocol/`; this
/// package exists from Phase 0 so the monorepo layout is stable.
library shared_models;
