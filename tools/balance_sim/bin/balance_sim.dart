/// Balance simulator CLI (§2.2 `tools/`). Fleshed out in Phase 1 alongside
/// `tools/bot_harness`, which it shares its win-rate reporting logic with.
void main(List<String> args) {
  // ignore: avoid_print
  print('balance_sim: see tools/bot_harness for the win-rate simulator.');
}
